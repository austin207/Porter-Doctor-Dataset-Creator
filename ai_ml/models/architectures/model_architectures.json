{
  "note": "Generated-style architecture source of truth is ai_ml/models/architectures/model_architectures.py. Run it with --summary to regenerate a full JSON summary.",
  "embedded_core": {
    "anomaly_detector": "tiny autoencoder over combined subsystem summary features",
    "router": "small MLP, Dense 32, Dense 16, softmax over power/motor/driver/esp32/lighting/unknown routes",
    "experts": "small MLP over sliding-window features, generally Dense 32 and Dense 16 with fault and action heads"
  },
  "pi_analyzer": {
    "sequence_embedding": "temporal autoencoder candidate",
    "clustering": "DBSCAN, HDBSCAN, or K-Means",
    "similarity_search": "embedding nearest-neighbor search",
    "root_cause_ranker": "ranked candidates from sequence evidence and similar confirmed events"
  }
}
